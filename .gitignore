# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# personal data (phone numbers etc.) — never commit
/data/contacts.json

# whatsapp-web.js session — this IS a live login to a real WhatsApp
# account. Committing it would let anyone with the repo send messages
# from that account. Never commit.
/.wwebjs_auth/
/.wwebjs_cache/

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
